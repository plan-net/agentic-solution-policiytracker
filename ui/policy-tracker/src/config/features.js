/**
 * Feature flags for PolicyTracker UI
 *
 * Set flags to true/false to enable/disable features.
 * This allows quick rollback of experimental features.
 */

export const FEATURES = {
  // Tool call visualization in chat messages
  // Shows tool calls as expandable icon badges instead of plain text
  TOOL_CALL_VISUALIZATION: true,
}
